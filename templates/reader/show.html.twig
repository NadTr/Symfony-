{% extends 'base.html.twig' %}

{% block title %}Reader{% endblock %}

{% block body %}
    <h1>Reader</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reader.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ reader.name }}</td>
            </tr>
            <tr>
                <th>Age</th>
                <td>{{ reader.age }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('reader.index') }}">back to list</a>

    <a href="{{ path('reader.edit', {'id': reader.id}) }}">edit</a>

    {{ include('reader/_delete_form.html.twig') }}
{% endblock %}
