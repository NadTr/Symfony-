{% extends 'base.html.twig' %}

{% block title %}Tea{% endblock %}

{% block body %}
    <h1 class="title">Tea</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tea.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ tea.name }}</td>
            </tr>
            <tr>
                <th>Origin</th>
                <td>{{ tea.origin }}</td>
            </tr>
            <tr>
                <th>Color</th>
                <td>{{ tea.color }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tea.index') }}">back to list</a>

    <a href="{{ path('tea.edit', {'id': tea.id}) }}">edit</a>

    {{ include('tea/_delete_form.html.twig') }}
{% endblock %}
